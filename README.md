# POP Platform (Final Working Version)
## Run Instructions:
1. Backend: `cd backend && npm install && npx prisma migrate dev --name init && npm run start:dev`
2. Frontend: `cd frontend && npm install && npm run dev`
3. Open http://localhost:5173 and Register/Login!
